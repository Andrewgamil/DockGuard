-- Sample seed data for URL shortener
-- This file can be used to populate the database with test data

-- Note: SQLite doesn't support INSERT IGNORE, so we'll use INSERT OR IGNORE
-- The actual seeding should be done through the application or a Python script

-- Example seed data (to be inserted via application):
-- INSERT INTO links (short_code, original_url, created_at, clicks) VALUES
-- ('abc123', 'https://example.com', datetime('now'), 0),
-- ('def456', 'https://github.com', datetime('now'), 0),
-- ('ghi789', 'https://stackoverflow.com', datetime('now'), 5);

